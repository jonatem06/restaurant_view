version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://backend:3000
    networks:
      - restaurant-network

networks:
  restaurant-network:
    driver: bridge
